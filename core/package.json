{
  "name": "@iptv-ninja/core",
  "version": "0.1.0",
  "private": true,
  "description": "Platform-agnostic IPTV core: Xtream Codes client, M3U/XMLTV parsers, shared types and storage abstraction. No React / React Native.",
  "license": "MIT",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "publishConfig": {
    "main": "dist/index.js",
    "types": "dist/index.d.ts"
  },
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run check:boundary && tsc -p tsconfig.test.json && node --test dist-test/test/*.test.js",
    "clean": "rm -rf dist dist-test",
    "check:boundary": "node scripts/check-boundary.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "~6.0.3"
  }
}
